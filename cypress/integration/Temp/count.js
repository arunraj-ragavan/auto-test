context('count', () => {
    beforeEach(() => {
      cy.visit('/about')
    })
  
    it('count links', () => {
      cy.get('');
    })
    it('has a table', () => {
      cy.get('ul');
        })
    it('has a list', () => {
            cy.get('table');
            cy.wait(5000);
              })
      })
  